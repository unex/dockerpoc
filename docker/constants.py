API_PATH = {
    "containers": "containers/json",
    "container_create": "containers/create",
    "container_start": "containers/{id}/start",
    "container_restart": "containers/{id}/restart",
    "container_stop": "containers/{id}/stop",
    "container_kill": "containers/{id}/kill",
    "container_pause": "containers/{id}/pause",
    "container_unpause": "containers/{id}/unpause",
    "container_remove": "containers/{id}"
}
